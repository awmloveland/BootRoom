# Implementation Prompt — 2.1 Collapse Dual Goalkeeper Representation

Paste this entire prompt as the opening message in a fresh Claude Code session.

---

You are refactoring the BootRoom 5-a-side league app so that "is this player a goalkeeper" lives in exactly one field on the runtime `Player` type.

**Working directory:** `/Users/willloveland/conductor/workspaces/bootroom/almaty`
**Branch:** `awmloveland/team-ratings-exploration`

## What you're refactoring

Today, `Player` carries both a `goalkeeper: boolean` flag and a `mentality` value where `'goalkeeper'` is one of the four enum options. The two can disagree (guest conversion in `resolvePlayersForAutoPick` sets `goalkeeper: true, mentality: 'balanced'`), and every consumer defensively checks both.

The fix: remove `Player.goalkeeper` entirely. The canonical signal becomes `mentality === 'goalkeeper'`. The DB column (if present) stays as legacy; translation happens at load time. `GuestEntry.goalkeeper` (at the UI entry level) stays — it's the only way the guest flow expresses keeper-ness without a mentality picker.

This is a pure refactor — no behavioural changes expected. All existing tests must pass unchanged (after fixture updates).

## Read first

1. **`CLAUDE.md`** — project conventions. Follow them.
2. **`docs/superpowers/specs/2026-04-21-team-building/2.1-goalkeeper-representation-spec.md`** — source of truth. Has the type-level changes, the audit procedure, and the test plan.

If anything here conflicts with the spec, follow the spec.

## How to approach it

Let the TypeScript compiler guide you.

1. **Audit first.** Run:
   ```
   grep -rn "goalkeeper" lib/ components/ app/ __tests__/
   ```
   Categorise every hit per the spec's six categories. You should end up with a list of files to edit and a list of sites that intentionally stay (`GuestEntry` reads, enum string literals, DB column references).

2. **Type changes.** Open `lib/types.ts`. Remove `goalkeeper: boolean` from `Player`. Remove `goalkeeper?: boolean` from `NewPlayerEntry`. Leave `GuestEntry.goalkeeper?: boolean` intact.

3. **Compile-driven rewrite.** Run `npx tsc --noEmit`. The compiler will complain at every read site. For each error:
   - Reading `p.goalkeeper` on a `Player` → replace with `p.mentality === 'goalkeeper'`.
   - Writing `goalkeeper: true` on a `Player` construction → remove and set `mentality: 'goalkeeper'` (if not already).
   - Writing `goalkeeper: false` on a `Player` construction → just remove the field.
   - Inside `resolvePlayersForAutoPick` (`components/NextMatchCard.tsx`) converting a `GuestEntry` → set the resulting `Player.mentality` based on `guest.goalkeeper`.
   - Inside the data layer (likely `lib/data.ts`) reading a DB row — translate: `mentality: row.goalkeeper ? 'goalkeeper' : row.mentality`. Preserve keeper identity on load.

4. **Test fixtures.** Walk every test file. Any Player-like object with `goalkeeper: true` → change to `mentality: 'goalkeeper'`. Any with `goalkeeper: false` → drop the field. Do this in one pass.

5. **Re-run the grep.** Remaining hits must be only:
   - `GuestEntry.goalkeeper` reads/writes (UI signal).
   - String literals `'goalkeeper'` (enum).
   - DB column references in the data layer.

6. **Run the full suite:** `npm test`. Any unexpected failure likely means a missed call site — fix it.

7. **Final typecheck:** `npx tsc --noEmit`. Should be clean.

## Constraints

- **Do not** drop `'goalkeeper'` from the `Mentality` enum.
- **Do not** remove `goalkeeper?: boolean` from `GuestEntry`.
- **Do not** alter any database schema or migration file.
- **Do not** change the behaviour of `ewptScore`, `wprScore`, `autoPick`, or any scoring logic. This is refactoring only — existing tests must pass with unchanged assertions.
- **Do not** add new helpers or files unless strictly needed to translate at a boundary.
- **Do** verify the data layer: if Supabase has a `goalkeeper` column, read it and translate to `mentality` on load. Do not break player loading.

## When you're done

Report in your final message:

1. The output of the initial `grep` (pre-audit enumeration).
2. The categorised list: which files you edited, which you intentionally left alone, and why.
3. A short code-diff summary per file (e.g., `lib/utils.ts: 3 reads updated, 1 write simplified`).
4. The output of the final `grep` — every remaining hit annotated as "kept intentionally (category 2/3/6)."
5. Confirmation that `npm test` and `npx tsc --noEmit` both pass (summary lines).

Do **not** commit or push. Leave changes staged for user review.
