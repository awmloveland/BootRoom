# Implementation Prompt — 2.2 Hoist Magic Numbers to Named Constants

Paste this entire prompt as the opening message in a fresh Claude Code session.

---

You are doing a readability refactor in the BootRoom 5-a-side league app: giving buried tunable parameters names, grouped and documented at the top of their files.

**Working directory:** `/Users/willloveland/conductor/workspaces/bootroom/almaty`
**Branch:** `awmloveland/team-ratings-exploration`

## What you're refactoring

`lib/autoPick.ts` and `lib/utils.ts` bury multiple tuning knobs as bare numbers mid-expression. This refactor hoists top-level tunables to named `const`s at the top of each file, leaving intertwined curve constants inline.

Pure refactor — no behavioural changes. All existing tests must pass unchanged.

## Read first

1. **`CLAUDE.md`** — project conventions.
2. **`docs/superpowers/specs/2026-04-21-team-building/2.2-named-constants-spec.md`** — source of truth. Has the proposed constant names, groupings, and an explicit scope rule for what to hoist vs leave.

If anything here conflicts with the spec, follow the spec.

## Prerequisites (critical)

This item **depends on items 1.1, 1.2, 1.3, and 2.1** being merged into the current branch first, because:

- **1.2** changes `ewptScore` weights from `0.65 / 0.10 / 0.25` to `0.90 / 0.10`.
- **1.3** changes the variety bonus to count outfielder mentalities only.
- **2.1** consolidates the goalkeeper representation.

Before starting, verify these have landed (`git log` for matching commits; grep for `avgForm` — should be absent post-1.2; grep for `Player.goalkeeper` — should be absent post-2.1). If they haven't, **STOP** and raise it with the user. Hoisting before they land would capture stale values that are about to change.

## How to approach it

1. Read the spec's constants list. Do not rename or retune any value — hoisting only.
2. In `lib/autoPick.ts`, add the constants block at the top (after imports). Substitute every matching literal in the file. Cross-check with `npm test` after each handful of substitutions.
3. In `lib/utils.ts`, add the constants block at the top (after imports). Substitute literals in `wprScore`, `ewptScore`, `winProbability`, and related helpers.
4. Leave curve-fitting constants inline per the spec's scope rule. Add a one-line comment above the formula if one isn't already there (e.g., the experience-penalty ramp).
5. Run the full suite: `npm test`. Any failure → a substitution is wrong. Find and fix.
6. Run typecheck: `npx tsc --noEmit`.

## Constraints

- **Do not** change any value. Literal `0.60` becomes `WPR_PPG_WEIGHT = 0.60` — exact preservation.
- **Do not** hoist the experience-penalty ramp constants (`0.85`, `0.03`, `1`, `5`).
- **Do not** hoist `winCopy` thresholds (`51`, `55`, `62`, `70`) — out of scope.
- **Do not** hoist `leagueWprPercentiles` fallback values (`40`, `50`, `60`) — out of scope.
- **Do not** add a new file unless explicitly needed. The spec recommends co-location.
- **Do not** change any behaviour or logic — `git diff` should show literals → names only (plus the new constants blocks).

## When you're done

Report in your final message:

1. The constants added per file (just the names — not the whole block).
2. The count of literal substitutions per file (e.g., "`lib/autoPick.ts`: 4 substitutions; `lib/utils.ts`: 17 substitutions").
3. Confirmation that `npm test` passed unchanged (no test edits needed) and `npx tsc --noEmit` passed.
4. Any judgment calls you made (e.g., "I included `POOL_EPSILON` but could argue either way"). One line each.

Do **not** commit or push. Leave changes staged for user review.
