# Implementation Prompt — 1.1 Form Denominator Fix

Paste this entire prompt as the opening message in a fresh Claude Code session.

---

You are implementing a small maths bug fix in the BootRoom 5-a-side league app.

**Working directory:** `/Users/willloveland/conductor/workspaces/bootroom/almaty`
**Branch:** `awmloveland/team-ratings-exploration`

## What you're fixing

The `wprScore` function in `lib/utils.ts` computes a player's recent-form percentage by dividing their earned weighted points by the maximum possible weighted points across 5 game slots. It currently treats `'-'` slots (games the player did not play) as if they were games where max points were possible — unfairly penalising any player with a short history. A player with `recentForm = 'WW---'` and a 100% win record currently scores 52.9% on form.

## Read first

1. **`CLAUDE.md`** — project conventions (Next.js 14, TypeScript strict, Tailwind, shadcn/ui patterns). Follow them.
2. **`docs/superpowers/specs/2026-04-21-team-building/1.1-form-denominator-spec.md`** — the source of truth for this change. It has the before/after table, the exact fix, and the test plan.

If anything in this prompt conflicts with the spec, follow the spec.

## How to approach it

Use test-driven development:

1. Open `lib/__tests__/utils.wpr.test.ts`. Add the new test cases listed in the spec's Test plan. Run them and confirm they fail against current code.
2. Apply the fix to `lib/utils.ts` as described in the spec.
3. Re-run the new tests — they should now pass.
4. Run the full test suite: `npm test`. Some previously-passing tests may now fail because their hardcoded expected values were computed under the old formula. Identify those, recompute the expected values under the new formula, and update them.
5. Run typecheck: `npx tsc --noEmit`.

## Constraints

- **Only** edit `lib/utils.ts` and `lib/__tests__/utils.wpr.test.ts`.
- Do **not** modify `playerFormScore` (around `lib/utils.ts:138–147`). It's being deleted under a separate item (1.2).
- Do **not** change any weights or thresholds elsewhere in `wprScore` (the 60/25/15 split, the 0.85–0.94 experience multiplier, the 0.88 rustiness multiplier, the 28-day threshold).
- Do **not** add new helpers, new files, or new dependencies.
- Preserve existing comments in `wprScore` unrelated to this change.

## When you're done

Report in your final message:

1. A code diff of the change in `lib/utils.ts` (should be small — a few lines).
2. The full list of test cases you **added** (names + expected values).
3. The full list of test cases you **updated** (name + old → new expected value).
4. Confirmation that `npm test` passed (include the summary line).
5. Confirmation that `npx tsc --noEmit` passed.

Do **not** commit or push. Leave the changes staged for the user to review.
