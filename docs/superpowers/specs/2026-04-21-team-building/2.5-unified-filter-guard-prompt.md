# Implementation Prompt — 2.5 Unify the Count-Balance Filter Guard

Paste this entire prompt as the opening message in a fresh Claude Code session.

---

You are making a small caller cleanup in the BootRoom 5-a-side league app: moving a redundant guard off the `autoPick` call site since the callee already handles the same case internally.

**Working directory:** `/Users/willloveland/conductor/workspaces/bootroom/almaty`
**Branch:** `awmloveland/team-ratings-exploration`

## What you're refactoring

`NextMatchCard.tsx`'s `handleAutoPick` passes `unknownNameSet.size >= 2 ? unknownNameSet : undefined` to `autoPick`. But `autoPick` already has an internal `if (unknownNames && unknownNames.size >= 2)` guard that no-ops cleanly for empty or singleton sets. The caller's ternary is redundant.

The fix: drop the ternary. Caller always passes the full set. `autoPick` owns the decision.

**Pure refactor — no behavioural change.** All existing tests must pass unchanged.

## Prerequisite

This item **depends on item 1.5 (extend count-balance filter to all unknowns)** being merged into the current branch. Verify by grepping for `unknownNameSet` in `components/NextMatchCard.tsx` — it should exist. If not, STOP and raise it with the user.

## Read first

1. **`CLAUDE.md`** — project conventions.
2. **`docs/superpowers/specs/2026-04-21-team-building/2.5-unified-filter-guard-spec.md`** — source of truth. Short spec — the change is a few lines.

If anything here conflicts with the spec, follow the spec.

## How to approach it

1. Open `components/NextMatchCard.tsx`. Find the `handleAutoPick` function. Locate the `autoPick(...)` call that takes `unknownNameSet.size >= 2 ? unknownNameSet : undefined` as the third argument. Replace with `unknownNameSet` directly.
2. (Optional) Open `lib/autoPick.ts` and expand the JSDoc above `autoPick`'s `unknownNames` parameter to note that passing an empty or singleton set is safe (filter no-ops). Include this only if it improves readability; skip if the existing doc already implies it.
3. Run the full suite: `npm test`. All tests must pass unchanged. Particular regression targets:
   - `autoPick — with 1 new player, does not apply filter and still returns valid suggestions`
   - `autoPick — passes no newPlayerNames — behaviour unchanged from baseline` (may have been renamed in 1.5).
4. Run typecheck: `npx tsc --noEmit`.

## Constraints

- **Do not** change the internal `size >= 2` threshold or any filter logic.
- **Do not** change any behaviour — same output splits before and after.
- **Do not** add new files or dependencies.
- **Do not** touch the data layer, scoring, split search, or any other autoPick internals.

## When you're done

Report in your final message:

1. A code diff of the change in `components/NextMatchCard.tsx` (small — one ternary removed).
2. Whether you updated the JSDoc in `lib/autoPick.ts` (and if so, show the diff).
3. Confirmation that `npm test` passed unchanged and `npx tsc --noEmit` passed.

Do **not** commit or push. Leave changes staged for user review.
