# Implementation Prompt — 1.5 Extend Count-Balance Filter to All Unknowns

Paste this entire prompt as the opening message in a fresh Claude Code session.

---

You are extending a post-generation filter in the BootRoom 5-a-side league app's auto-pick algorithm so it covers guests as well as new players.

**Working directory:** `/Users/willloveland/conductor/workspaces/bootroom/almaty`
**Branch:** `awmloveland/team-ratings-exploration`

## What you're fixing

`autoPick` in `lib/autoPick.ts` has a count-balance filter that stops "unknowns" from clustering on one team. Today it only counts **new players** as unknowns. Guests — someone's `+1` — are equally unknown from a rating-confidence perspective and should be included. Fix: rename the parameter `newPlayerNames` → `unknownNames` and update `NextMatchCard` to build the set from both `guestEntries` and `newPlayerEntries`.

## Read first

1. **`CLAUDE.md`** — project conventions. Follow them.
2. **`docs/superpowers/specs/2026-04-21-team-building/1.5-unknown-count-balance-spec.md`** — source of truth for this change. Has the full scenarios, before/after code, and test plan.

If anything in this prompt conflicts with the spec, follow the spec.

## How to approach it

Test-driven:

1. Open `lib/__tests__/autoPick.test.ts`. Add the new test cases described in the spec's Test plan (mixed unknowns with unbalanced guests; mixed unknowns single count; guest fallback path; backward compatibility). Run; the unbalanced-guests test should fail against current code.
2. Rename the parameter in `lib/autoPick.ts` from `newPlayerNames` to `unknownNames`. Update the JSDoc block above the function. Update the internal variable references.
3. In `components/NextMatchCard.tsx` (inside `handleAutoPick`), replace the `newPlayerNameSet` construction with `unknownNameSet`, built from BOTH `guestEntries` and `newPlayerEntries`. Preserve the `size >= 2` guard at the call site — item 2.5 will unify this later.
4. Rename the existing describe block in the test file from `autoPick — newPlayerNames count-balance filter` to `autoPick — unknownNames count-balance filter`. Rename local variables (`newPlayerNameSet` → `unknownNameSet`) in the existing tests' bodies.
5. Run the new tests — they should now pass.
6. Run the full suite: `npm test`. All existing tests should still pass after the rename; fix any that break due to typos.
7. Run typecheck: `npx tsc --noEmit`.

## Constraints

- **Only** edit `lib/autoPick.ts`, `components/NextMatchCard.tsx`, and `lib/__tests__/autoPick.test.ts`.
- Do **not** change the filter's slack (`<= 1`) or its fallback-to-unfiltered logic.
- Do **not** change the pair-pinning algorithm — it still takes precedence over the count filter.
- Do **not** change the call-site guard structure. Item 2.5 will move the guard inside `autoPick` later; preserve the current "pass `undefined` when size < 2" pattern at the call site.
- Do **not** add new files or dependencies.

## When you're done

Report in your final message:

1. A code diff of the changes in `lib/autoPick.ts` (rename only — small).
2. A code diff of the changes in `components/NextMatchCard.tsx` (the `unknownNameSet` construction).
3. The full list of test cases you **added** (names + expected behaviour).
4. The full list of existing tests you **renamed** or **updated** due to the parameter rename.
5. Confirmation that `npm test` passed (summary line) and `npx tsc --noEmit` passed.

Do **not** commit or push. Leave changes staged for user review.
