# Implementation Prompt — 1.4 Randomise Odd-Player Allocation

Paste this entire prompt as the opening message in a fresh Claude Code session.

---

You are implementing a small fairness fix to the team-split logic in the BootRoom 5-a-side league app.

**Working directory:** `/Users/willloveland/conductor/workspaces/bootroom/almaty`
**Branch:** `awmloveland/team-ratings-exploration`

## What you're fixing

`autoPick` in `lib/autoPick.ts` (around line 111) always rounds Team A's size up when the squad has an odd player count (n = 11 → 6 on Team A, 5 on Team B, every single run). This gives Team A a persistent numerical advantage plus a ~0.5-point depth bonus. Fix: coin-flip which team receives the extra slot when n is odd.

## Read first

1. **`CLAUDE.md`** — project conventions. Follow them.
2. **`docs/superpowers/specs/2026-04-21-team-building/1.4-odd-player-allocation-spec.md`** — source of truth for this change. Has the before/after code, test plan, and edge-case notes.

If anything in this prompt conflicts with the spec, follow the spec.

## How to approach it

Test-driven:

1. Open `lib/__tests__/autoPick.test.ts`. Add the three new test cases described in the spec's Test plan (odd-count distribution, even-count invariance, odd-count with pinned GKs). The distribution tests will fail against current code (100% concentration on Team A for odd n).
2. Apply the change in `lib/autoPick.ts` (around line 111):
   - Add the `extraSlotToA` coin-flip variable.
   - Add the `halfSize` variable.
   - Use `halfSize` in the existing `sizeA` calculation.
   - Keep the `Math.max(0, Math.min(searchPool.length, ...))` clamp unchanged.
3. Run the new tests — they should now pass.
4. Run the full suite: `npm test`. Any existing test that asserts a specific `teamA.length` when n is odd will either fail outright or become flaky. For each:
   - If the assertion is `teamA.length + teamB.length === n`, no change needed.
   - If the assertion is `teamA.length === <specific value derived from ceil(n/2)>`, relax it to accept either `ceil(n/2)` or `floor(n/2)` (net of pinned counts).
5. Run typecheck: `npx tsc --noEmit`.

## Constraints

- **Only** edit `lib/autoPick.ts` and `lib/__tests__/autoPick.test.ts`.
- Do **not** change the `Math.max(0, Math.min(searchPool.length, ...))` clamp logic.
- Do **not** change the GK pinning, pair pinning, or pool scoring logic.
- Do **not** change `depthBonus` or any `ewptScore` internals — those live in `lib/utils.ts` and are out of scope.
- Do **not** add new files or dependencies.
- The 100-run distribution tests use soft bounds (35/65). Don't tighten them — `Math.random()` can produce short streaks. Flaky tests are worse than soft bounds.

## When you're done

Report in your final message:

1. A code diff of the changes in `lib/autoPick.ts` (small — two new variables plus one substitution).
2. The full list of test cases you **added** (names + expected behaviour).
3. The full list of existing test cases you **updated** (name + reason for the relaxation).
4. Confirmation that `npm test` passed (summary line) and `npx tsc --noEmit` passed.
5. The observed distribution from your 100-run odd-count test (e.g., "47 runs with extra slot to A, 53 runs with extra slot to B") — a sanity check that the coin flip is fair.

Do **not** commit or push. Leave changes staged for user review.
