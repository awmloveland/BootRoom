# Implementation Prompt — 2.3 Refactor Pair-Pinning with a Placement Helper

Paste this entire prompt as the opening message in a fresh Claude Code session.

---

You are extracting a small helper in the BootRoom 5-a-side league app's auto-pick algorithm to collapse five parallel branches in the pair-pinning loop into a single lookup.

**Working directory:** `/Users/willloveland/conductor/workspaces/bootroom/almaty`
**Branch:** `awmloveland/team-ratings-exploration`

## What you're refactoring

`autoPick`'s pair-pinning loop (lib/autoPick.ts:65–108) has five parallel branches for "where is the associated player?" — each a near-copy of the others. This refactor extracts a `findAssocTeam(name, ...)` helper returning `'A' | 'B' | null`, collapsing the branches to a single call.

**Pure refactor — no behavioural change.** Existing pair tests must pass unchanged.

## Read first

1. **`CLAUDE.md`** — project conventions.
2. **`docs/superpowers/specs/2026-04-21-team-building/2.3-pair-pinning-helper-spec.md`** — source of truth. Has the helper signature, the rewritten loop, and the critical behaviour to preserve.

If anything here conflicts with the spec, follow the spec.

## Critical behaviour to preserve (read twice)

- `pairTeamToggle` flips **only** in the "associated player found in free pool" case. Follower cases (GK, already placed) do **not** advance the toggle.
- Guests whose associated player is absent stay in `searchPool` — they get distributed freely by the subsequent split search.
- Pool mutations and team placements are byte-for-byte identical to the 5-branch version.

If a pair test that currently passes suddenly fails, it's almost certainly one of these three behaviours that slipped. Check them first.

## How to approach it

Test-driven:

1. Open `lib/__tests__/autoPick.test.ts`. Add the helper unit tests from the spec (null return, each positive case, precedence). Run; they'll fail because `findAssocTeam` doesn't exist.
2. Add the `findAssocTeam` helper in `lib/autoPick.ts` and export it so the tests can import it.
3. Run the helper tests — they should pass.
4. Rewrite the pair-pinning loop in `autoPick` per the spec. The "associated found in pool" branch flips the toggle and assigns by toggle; the "associated not found" branch calls `findAssocTeam` and places the guest accordingly.
5. Run the full suite: `npm test`. All existing pair tests should pass unchanged. If any fail, that's a behavioural regression — don't claim done until they pass.
6. Run typecheck: `npx tsc --noEmit`.

## Constraints

- **Do not** change any behaviour. Same placements, same pool, same toggle fingerprints.
- **Do not** change the GK pinning loop that runs *before* the pair loop.
- **Do not** address the O(n²) `searchPool.filter(...)` pattern — that's item 2.4.
- **Do not** change `ewptScore`, `wprScore`, or any scoring logic.
- **Do not** add new files or dependencies.

## When you're done

Report in your final message:

1. A code diff of the changes in `lib/autoPick.ts` (new helper + rewritten loop).
2. The full list of helper unit tests you added.
3. Explicit confirmation that every pre-existing pair test passed unchanged.
4. Confirmation that `npm test` passed overall (summary line) and `npx tsc --noEmit` passed.

Do **not** commit or push. Leave changes staged for user review.
