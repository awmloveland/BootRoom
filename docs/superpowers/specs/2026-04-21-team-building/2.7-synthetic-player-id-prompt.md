# Implementation Prompt — 2.7 Synthetic Player ID at Resolution Boundary

Paste this entire prompt as the opening message in a fresh Claude Code session.

---

You are replacing name-as-identity with a synthetic `playerId` field in the BootRoom 5-a-side league app's auto-pick pipeline. Name stays on Player objects for display; identity comparisons move to the stamped ID.

**Working directory:** `/Users/willloveland/conductor/workspaces/bootroom/almaty`
**Branch:** `awmloveland/team-ratings-exploration`

## What you're changing

`autoPick` and its call sites use `p.name` as identity for pair matching, the count-balance filter, and swap-dedup keys. Two players sharing a name silently collide. Fix: add a required `playerId: string` on `Player`, stamped at the resolution boundary with type-prefixed IDs (`known|<name>`, `guest|<name>`, `new|<name>`, or `roster|<dbId>` in the data layer). Internal identity comparisons use `playerId`. External API (`pairs` as `Array<[string, string]>`) stays name-based and is resolved to IDs internally.

**Scope is broad** — widest code-quality refactor of the 2.x tier. Follow the spec carefully.

## Prerequisites (verify before starting)

This item depends on 2.1, 2.3, 2.4 having landed. Check:

```
grep -n "findAssocTeam" lib/autoPick.ts                    # must exist (2.3)
grep -n "excluded = new Set" lib/autoPick.ts               # must exist (2.4)
grep -En "\bgoalkeeper:\s*boolean" lib/types.ts            # must be absent on Player (2.1)
```

If any check fails, STOP and raise it with the user.

## Read first

1. **`CLAUDE.md`** — project conventions.
2. **`docs/superpowers/specs/2026-04-21-team-building/2.7-synthetic-player-id-spec.md`** — source of truth. Has the full stamping strategy, internal substitutions, test plan, and files-changed list.

If anything here conflicts with the spec, follow the spec.

## How to approach it

1. **Type change first.** In `lib/types.ts`, add `playerId: string` as a required field on `Player`. Run `npx tsc --noEmit`. Every Player construction site that doesn't set the field becomes a compile error — use that as your work list.

2. **Stamp at boundaries.** For each TS error:
   - `components/NextMatchCard.tsx` → `resolvePlayersForAutoPick`: stamp `known|<name>`, `guest|<name>`, or `new|<name>` per the spec.
   - `lib/data.ts` (if it returns `Player[]`): stamp `roster|<dbId>` if the DB primary key is accessible; otherwise `known|<name>`. Note which you chose in your report.
   - Test fixture helpers (`makePlayer`, etc.): default to `known|<name>`.

3. **Update `autoPick` internals:**
   - At the top of `autoPick`, build `nameToId: Map<string, string>` from `players`. Translate `pairs` into ID-based `idPairs`.
   - Update `findAssocTeam` to take `assocId: string | undefined`. Compare against `.playerId` in each branch.
   - Update the count-balance filter: parameter becomes `unknownIds: Set<string>`, membership check `unknownIds.has(p.playerId)`.
   - Update swap-dedup key construction to use `p.playerId`.
   - `excluded: Set<Player>` from 2.4 — keep as-is or convert to `Set<string>` (IDs). Either is fine; call it out in your report.

4. **Update call site.** `components/NextMatchCard.tsx` → `handleAutoPick`: build `unknownIds` from the resolved Player array filtered by whose `name` matches a guest or new-player entry. Pairs stay name-based at the call site.

5. **Test fixtures.** Update `makePlayer` and any similar helpers to stamp `playerId`. Run the full suite.

6. **Add collision tests** (new):
   - Two Players with same name, different `playerId` → each appears exactly once across suggestions.
   - Guest "Alice" (`guest|Alice`) and known "Alice" (`known|Alice`) → treated as distinct.

7. `npm test` — everything should pass.

8. `npx tsc --noEmit` — clean.

## Constraints

- **Do not** change the external `autoPick` signature's `pairs` parameter type. It stays `Array<[string, string]>` (names in, resolved to IDs at the top).
- **Do not** remove `name` from the Player type. Display uses it.
- **Do not** change `wprScore`, `ewptScore`, or any scoring logic.
- **Do not** make `playerId` optional. Required field only — the point is discipline.
- **Do not** create a new `lib/playerId.ts` file. Stamping lives at boundaries (`resolvePlayersForAutoPick`, `lib/data.ts`, test helpers).

## When you're done

Report in your final message:

1. A diff summary of type changes (`lib/types.ts`).
2. Every location where `playerId` is stamped + the prefix convention (e.g., "`resolvePlayersForAutoPick`: `known|<name>` / `guest|<name>` / `new|<name>`"; "`lib/data.ts`: `roster|<row.id>` — DB ID was available"). One line each.
3. A high-level diff of `lib/autoPick.ts` (pair resolution added; helper updated; identity substitutions).
4. The new collision tests added and their assertions.
5. Confirmation that `npm test` and `npx tsc --noEmit` both pass.
6. Any judgment calls you made (e.g., "kept `excluded: Set<Player>` rather than converting to `Set<string>` because the conversion cost more touching"). One line each.

Do **not** commit or push. Leave changes staged for user review.
